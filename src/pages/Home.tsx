
// Home -> componente = PascalCase
// Es una función que no recibe props (paréntesis vacíos) y devuelve JSX.
const Home = () => {
  return (
    //Todo lo que hay entre ( y ) es JSX (parecido a HTML, pero dentro de JS).
    <section>
      <h1>Adison</h1>
      <p>Analista datos</p>
      <p>Bienvenido a mi portafolio</p>
    </section>
  )
}

export default Home;
//Exportación por defecto 